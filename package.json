{
  "name": "hono-server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build --target=bun --outfile=dist/server.js src",
    "start": "bun dist/server.js"
  },
  "dependencies": {
    "hono": "^4.10.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
